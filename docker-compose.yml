version: '3.8'

services:
  db:
    image: postgres:15
    container_name: agendar_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: pgpassword
      POSTGRES_DB: agendar_db
    ports:
      - "5433:5432"
    volumes:
      - ./data:/var/lib/postgresql/data